export function CultureEvents() {
  return (
    <section
      id="events"
      className="w-full min-h-[40vh] flex items-center justify-center px-6 py-24"
      style={{
        background: 'linear-gradient(180deg, #0B0F2B 0%, #1C0B46 50%, #0B0F2B 100%)',
      }}
    >
      {/* TODO: Culture Events section — no visual direction finalized yet, build after hero is approved */}
      <div className="text-center opacity-30">
        <p className="font-accent font-bold text-soft-lilac text-xl uppercase tracking-widest">
          Events
        </p>
        <p className="font-sans text-silver/50 text-sm mt-3">
          Section under development
        </p>
      </div>
    </section>
  );
}
